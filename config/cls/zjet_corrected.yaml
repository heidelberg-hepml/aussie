# @package _global_
defaults:
  - /cls
  - override /dataset: zjet_corrected
  - _self_

net:
  dim_in: ${dataset.process.num_features}
  dim_out: 1
  ensembled: false

training:
  optimizer:
    warmup_steps: 5000
    weight_decay: 0.0