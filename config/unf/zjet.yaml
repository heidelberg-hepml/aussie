# @package _global_
defaults:
  - /unf
  - _self_

net:
  dim_in: ${dataset.process.num_features}
  dim_out: 1
  ensembled: 20

training:
  lr: 5e-3
  optimizer:
    warmup_steps: 5000
    betas: [0.95,0.999]